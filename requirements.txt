Flask==2.3.2
streamlit==1.24.0
sendgrid==6.10.1
smtplib==3.11  # Note: smtplib is part of Python's standard library, so it doesn't need to be added to requirements.txt
langchain-groq==0.1.0  # Adjust the version if necessary (since langchain_groq isn't available on PyPI, verify this)
celery==5.2.7
google-auth==2.22.0
google-auth-oauthlib==0.8.0
google-api-python-client==2.77.0
pandas==2.1.1
plotly==5.15.0
